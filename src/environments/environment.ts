// Environment configuration for development mode.
// These values are replaced at build time based on the target environment (via Angular's fileReplacements).
export const environment = {
  production: false, // Indicates this is the development environment
  apiBaseUrl: 'https://localhost:7168/api' // Base URL for API requests during development
};
